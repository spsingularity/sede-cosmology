# Reproduce the figures (reproduce_figures.py) with the vendored `sede` package.
numpy>=1.23
scipy>=1.9
matplotlib>=3.6
# Optional — only for fig_e1_mechanism (CAMB-in-the-loop distance calc):
# camb>=1.5
